# C6/C7 双端动效与同步状态核查

日期：2026-07-19
网页端：`v2.15.1`
小游戏端：`v2.18.0`

## 同步矩阵

| 项目 | 网页版 | 小游戏 | 状态 |
| --- | --- | --- | --- |
| 按钮反馈 | CSS hover/active 过渡 | Canvas 130ms 中心缩放按压态 | 交互意图同步 |
| 弹窗 | DOM/CSS 渐入与缩放 | Canvas 弹窗阶段性重绘 | 功能同步，呈现方式不同 |
| 当前玩家 | `turn-changed` 反馈 | HUD 颜色高亮与衰减 | 同步 |
| 棋子移动 | rAF/CSS 路径与飞行反馈 | Canvas 分段路径与跳跃弧线 | 同步 |
| 任务卡 | DOM 弹窗卡面 | Canvas 任务卡/国王卡 | 同步 |
| 骰子 | `v2.15.1` 横向抛出、两段弹跳、阴影、光圈；竖屏位置单独修订 | `v2.17.0` 后续已同步同一运动契约 | 同步，布局按平台独立 |
| 低动态 | `reduced-motion` 降低过渡 | `reducedMotionEnabled` 关闭非必要按压/HUD装饰 | 策略同步 |

## 可接受差异

- 网页使用 DOM、CSS 3D 和 `requestAnimationFrame`；小游戏使用 Canvas、定时重绘和绘制变换。
- 两端可见版本号暂不相同：网页为 `v2.15.1`，小游戏为 `v2.18.0`。这是分阶段单端迭代，不表示玩法、骰子最终点数或存档契约不同。
- 动效时长和缓动细节可按平台调整，但不得改变按钮命中范围、骰子唯一 transaction、最终点数、棋盘/任务/存档规则。

## C7 结论

- 共享玩法、棋盘坐标、任务触发、存档结构和素材路径：本轮无待同步改动。
- C5.1 骰子动效已由小游戏 `v2.17.0` 承接；本记录用于闭合早期“小游戏待同步”的历史状态，不回改旧文档，避免改动已完成任务的归档说明。
- C6 新增的是小游戏触摸按压态，网页已有等价的 CSS active 反馈，因此不需要改 `web/`。
- 网页 `v2.15.1` 只修正竖屏 DOM/CSS 骰子台位置，小程序 Canvas 布局无需同步。
- C2-C7 累计发布说明已补充自由昵称与云安全审核；该功能只存在于小游戏端，不属于本次网页版同步项。

## 网页版同步包

【网页版同步包】

本次小程序已改内容：

- `minigame/game.js` 的通用 Canvas 按钮增加轻量按压反馈。

网页版需要同步：否
同步目标文件：无

关键规则/素材路径：

- 不涉及骰子结果、棋盘坐标、任务、存档或素材。
- 网页端已存在 CSS `:active` 按钮反馈。

验证方法：

- 保持网页端按钮点击、弹窗、玩家切换、棋子和骰子现有检查通过即可。

注意事项：

- 不要因本次小游戏端视觉反馈修改 `web/`。
- 后续若统一具体时长或曲线，须单独建立双端动效任务并分别验证。
