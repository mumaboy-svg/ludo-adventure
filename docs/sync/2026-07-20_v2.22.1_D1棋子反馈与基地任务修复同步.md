# v2.22.1 D1 棋子反馈与基地任务修复同步

日期：2026-07-20
网页版：`v2.22.1`
微信小游戏：`v2.22.1`

## 双端同步状态

| 内容 | 网页版 | 微信小游戏 | 状态 |
| --- | --- | --- | --- |
| 当前棋子玩家色光环 | DOM/CSS | Canvas | 已同步 |
| 移动阴影与姿态 | DOM/CSS 动画 | Canvas 瞬态绘制 | 已同步 |
| 落地扩散与回弹 | DOM/CSS 动画 | Canvas 瞬态绘制 | 已同步 |
| 低动态降级 | 静态当前态 | 静态当前态与缩短移动 | 已同步 |
| 基地任务重复点数 | 网页原逻辑已为每次触发 | 移除永久去重 | 规则已同步 |

## 小程序口同步说明

【小程序口同步包】

网页版 D1 棋子反馈已同步到小游戏：

- 当前棋子使用玩家色光环。
- 移动中显示贴地阴影与轻量姿态变化。
- 落地显示扩散环和一次回弹。
- 低动态模式关闭非必要动画。

小游戏额外修复：

- 基地里的棋子每次掷出 `2、3、4、5` 都弹出对应任务。
- 同一棋子重复掷到相同点数也必须再次触发。
- 不再读取或写入 `base-棋子-点数` 永久去重键。
- 旧存档字段保持兼容，延迟弹窗队列保持不变。

## 验证重点

- 红、黄、蓝、绿当前棋子光环清楚且不遮挡棋盘。
- 普通移动、起飞和飞跃都有移动阴影与落地反馈。
- 开启低动态后不出现持续浮动或落地扩散。
- 基地连续两次掷出相同 `2-5` 点数时，两次都出现任务卡。
- `1` 点国王卡、`6` 点起飞流程、骰子结果、移动与存档行为保持原样。

## 发布边界

- 不修改任务文本、国王卡、棋盘、骰子 transaction、胜利规则、存档 schema、云存档或素材。
- 微信小游戏只上传体验版，不提交审核、不正式发布。

## 实际发布回执

- 正式源码提交：`9d3d384`。
- GitHub 协作分支和 `main` 已同步，GitHub Pages 已显示网页版 `v2.22.1`。
- 微信小游戏体验版已上传 `v2.22.1`，AppID `wx03730ae0403a92d3`。
- 包体：总包 `4,452,764 B`，主包 `2,410,404 B`，资源分包 `2,042,360 B`。
- 回执目录：`reports/wechat-upload-2026-07-20-v2.22.1-d1-base-task/`，仅本地留存，不提交 Git。
