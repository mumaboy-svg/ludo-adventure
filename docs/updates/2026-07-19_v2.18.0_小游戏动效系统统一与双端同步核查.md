# v2.18.0 小游戏动效系统统一与双端同步核查

日期：2026-07-19
版本评估：小游戏 `v2.17.0` -> `v2.18.0`。本次新增玩家可见的通用 Canvas 按钮按压反馈；C 组收尾时网页版另行修订为 `v2.15.1`。

## 修改文件

- `minigame/game.js`
- `minigame/tests/dice_c5_test.js`
- `minigame/tests/motion_c6_test.js`
- `docs/updates/2026-07-19_v2.18.0_小游戏动效系统统一与双端同步核查.md`
- `docs/sync/2026-07-19_v2.18.0_C6C7双端动效与同步状态核查.md`

## C6 完成内容

- 通用 Canvas 菜单、工具、面板、开关和危险操作按钮接入 130ms 轻量按压反馈。
- 反馈只改变绘制变换；命中测试仍使用原始 `{ x, y, w, h }` 矩形，不改变触控边界。
- 低动态模式关闭按压缩放。
- 保留既有弹窗弹入、当前玩家 HUD 高亮、棋子移动、任务卡展示和 `v2.17.0` 骰子抛出/弹跳/阴影。

## C2-C7 累计内容补充

- C2：当前玩家 HUD 集中显示玩家、轮次、位置和上次点数。
- C3：任务中心卡册化，并补充模式、玩家数、存档状态、牌组摘要和开始入口。
- C5.1：实体骰子加入横向抛出、两段弹跳、动态阴影和两次落地反馈。
- C6/C7：按钮按压反馈、低动态保护和双端同步矩阵完成。
- 同期累计纳入自由昵称与云安全审核：支持 1-12 个字符、受限内容过滤、审核标记、可信默认昵称和旧存档兼容。该内容此前未在各阶段说明中完整披露，本文件作为 `v2.18.0` 累计发布说明补齐。

## 高风险未改范围

- `web/` 代码和网页版版本号。
- 骰子唯一 transaction、最终点数显示、随机源与结算顺序。
- 棋盘坐标、直道入口、终点反弹、任务触发和任务内容。
- 本轮 C6 不改变存档结构、昵称审核算法、云函数和素材路径；但累计 `v2.18.0` 发布集包含此前已完成的昵称云审核改造，见上节。

## 验证

- `node --check minigame/game.js`
- `node minigame/tests/dice_c5_test.js`（仅同步版本断言，骰子契约断言未变）
- `node minigame/tests/motion_c6_test.js`
- 网页内联脚本语法检查。
- `git diff --check` 目标文件。
- 按压反馈以原始按钮边界命中，适配竖屏、矮屏和横屏绘制区域；微信开发者工具测试预览仅用于验收，不上传。

## 外部核查证据

- Mavis：任务 `20260719_105621_20260719_C6C7_双端动效与同步状态最终核查`，路由 `dual-end-audit`；报告及严格 `.done.json` 已核对，作为双端差异矩阵候选采纳。其“小游戏缺少按钮按压态”的发现已在本轮修复；其余建议按范围记录，不扩展实施。
- QClaw：任务 `LUDO-QCLAW-20260719-105621-LUDO-QCLAW-20260719-C6C7-双端动效同步最终核查`，通过固定路由发送后 `TimeoutError`，没有可采纳报告。用户反馈通道偶发卡住后，另发真实诊断任务 `LUDO-QCLAW-20260719-111103-C6C7_QClaw_通道卡住诊断与恢复建议`；其同样在 120 秒内 `TimeoutError`，因此只记录失败回执，不推断诊断结论。
- Codex：以当前工作树完成实现、静态契约检查、语法检查和预览验收。

## 备份与发布状态

- 备份：`backups/2026-07-19_v2.17.0_before-c6c7-motion-sync/game.js.before`
- 微信开发者工具已将小游戏 `v2.18.0` 上传为体验版，回执位于 `reports/wechat-upload-2026-07-19-v2.18.0-c6c7-motion-sync/upload-info.json`。
- 尚未提交微信审核、尚未正式发布。
- C 组收尾任务负责将白名单源码、测试和说明同步到 GitHub；不纳入备份、代理报告、临时截图和无关文件。

## C7 状态

双端动效矩阵、已同步项、可接受的平台差异和网页版同步包见 `docs/sync/2026-07-19_v2.18.0_C6C7双端动效与同步状态核查.md`。
