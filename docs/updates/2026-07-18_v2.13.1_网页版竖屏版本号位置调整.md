# v2.13.1 网页版竖屏版本号位置调整

## 版本评估

- 类型：小范围视觉修订。
- 版本：网页版 `v2.13.0` 升级为 `v2.13.1`。
- 范围：仅网页版竖屏大厅的版本号与存档状态布局。
- 评估依据：不新增功能、不修改玩法，只调整一个状态角标的位置与视觉层级，因此升级修订版本。

## 修改文件

- `web/index.html`
- `CHANGELOG.md`
- `docs/updates/2026-07-18_v2.13.1_网页版竖屏版本号位置调整.md`

## 修改内容

- 竖屏大厅右上角只保留存档状态。
- 版本号缩小并放到存档状态正下方，形成清晰的纵向信息组。
- 移除纯网页版竖屏中多余的 `82px` 小程序胶囊预留。
- 同步更新网页加载页、页面标题和大厅角标使用的版本号为 `v2.13.1`。
- 横屏大厅继续使用原有布局，不受本轮竖屏专属样式影响。

## 未修改内容

- 未修改 `minigame/`。
- 未修改骰子、棋盘、任务、玩家设置、存档结构、音效或游戏规则。
- 未继续 C5 骰子专项工作。

## 助手分工

- QClaw 完成修改前只读检查，确认原布局没有功能遮挡，但网页版右侧 `82px` 胶囊预留冗余。
- Codex 负责位置方案、正式修改、版本评估与最终验收。

## 备份

- `backups/2026-07-18_v2.13.1_before-portrait-version-position/`
- 修改前 Git 基线：`77d5836`

## 测试结果

- 网页内联脚本语法检查通过。
- `390×844` 与 `375×667` 竖屏视口检查通过。
- 版本号与存档状态、Logo、主按钮、底部工具无重叠。
- 页面无横向溢出，竖屏首屏高度保持不变。
- 桌面横屏样式保持原有规则。
- `git diff --check` 通过。

## 小程序同步说明

- 本轮是纯网页版竖屏角标位置调整，小程序使用 Canvas 和原生胶囊安全区布局，不应照搬网页版 CSS。
- 小程序端无需修改代码，继续保持 `v2.12.0`。
- 本轮未生成双端同步包。

## 发布状态

- 已完成本地修改与验证。
- 未执行 Git 暂存、提交、推送或 GitHub Pages 发布。
