# C3 网页版任务中心实现与验收说明

## 实现范围

- 文件：`web/index.html`
- 任务卡册视觉系统。
- 五种模式卡片和选中状态。
- 预设模式与手动任务工坊的布局切换。
- 任务概览、模式状态、开局区和工具区重组。
- 手机双列和矮横屏响应式适配。
- 任务中心入口滚动位置重置。

## 明确排除

- 微信小游戏 Canvas 实现。
- 任务内容、任务字段和任务抽取规则。
- 棋盘、骰子、移动、胜利和存档结构。
- 本实现阶段不包含 Git 提交、推送和线上发布；后续由独立发布任务执行。

## 契约核对

| 检查项 | 结果 |
| --- | --- |
| 原有任务中心 ID | 保留 |
| 五个 `data-task-mode` 值 | 保留 |
| 模式按钮事件绑定 | 保留 |
| 任务导入文件输入 | 保留 |
| 保存、导出、检查和示例入口 | 保留 |
| 手动模式编辑区 | 保留 |
| 任务及存档契约 | 未修改 |

## 运行时验收

| 视口或功能 | 结果 |
| --- | --- |
| `1366×768` | 通过，预设卡册居中，无横向溢出 |
| `844×390` | 通过，顶部完整，紧凑五列，无横向溢出 |
| `390×844` | 通过，双列布局，手动任务卡占整行 |
| `375×667` | 通过，小屏允许纵向滚动，无横向裁切 |
| 五种模式切换 | 通过 |
| 编辑区显示/隐藏 | 通过 |
| 任务读取入口 | 通过，按钮和文件输入事件保持连接 |
| 返回大厅、玩家设置、开局 | 通过 |
| 浏览器页面脚本错误 | 无 |

控制台唯一的 404 来自浏览器自动请求 `/favicon.ico`，不是游戏资源，不影响功能。

## 静态检查

- 网页工作版本：`2.12.0`。
- 小游戏版本：`2.11.1`。
- 网页内联脚本语法检查通过。
- `git diff --check -- web/index.html` 通过。

## 验收门禁

当前实现和技术验收已完成，并已由独立发布任务并入 Web `v2.13.0` Git 发布。
