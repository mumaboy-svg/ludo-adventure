# v2.7.11 网页版棋盘7直道颜色重映射

日期：2026-07-16
范围：网页版直道坐标颜色映射

## 新映射

- `R1-6`：使用原 `Y1-6` 的右侧横向直道坐标。
- `B1-6`：使用原 `R1-6` 的顶部纵向直道坐标。
- `Y1-6`：使用原 `G1-6` 的左侧横向直道坐标。
- `G1-6`：使用原 `B1-6` 的底部纵向直道坐标。

## 影响范围

- 直道数字标签跟随新颜色显示。
- 对应颜色棋子进入直道后沿新颜色直道移动。
- 直道反弹和直道任务仍按原 `straightIndex` 规则执行。

## 未修改内容

- 不修改外圈 52 格坐标。
- 不修改机场、起飞点、直道入口和特殊飞跃规则。
- 不修改任务内容、骰子、音频或存档结构。
- 不修改 `minigame/` 小程序文件。

## 版本

- 网页版：`v2.7.11`
- 小程序：保持 `v2.7.8`
