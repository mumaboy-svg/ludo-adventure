# C4 网页版设置系统实现与验收说明

## 基本信息

- 任务：`LUDO-VISUAL-20260718-V2130-C4-SETTINGS-MENU`
- 网页发布版本：`2.13.0`
- 微信小游戏版本：`2.11.1`
- 范围：仅网页版设置系统。
- Git 状态：随 Web `v2.13.0` 发布提交推送。
- 修改前备份：`backups/2026-07-18_1533_before-v2130-settings-menu/`

## 实现范围

- 设置页完整菜单化的信息层级。
- 玩家配置、声音与画面、数据管理三类导航。
- 桌面端侧边导航和移动端顶部标签。
- 玩家数量、名称、颜色和存档状态展示。
- 玩家配置独立保存和可见反馈。
- 背景音乐与动态效果开关。
- 任务、存档和开局覆盖状态提示。
- 恢复默认和清空全部数据的风险分区。
- 继续上局、保存设置和开始新游戏的统一底部操作区。
- 手机竖屏和 `844×390` 矮横屏响应式适配。

## 明确排除

- 微信小游戏 Canvas 设置页实现。
- 骰子随机、点数显示和动画。
- 棋盘图片、坐标及任何玩法规则。
- 任务内容、任务抽取和任务触发。
- 胜利判断。
- 现有游戏存档键和存档结构变更。
- 本实现阶段不包含 Git 暂存、提交、推送和线上发布；后续由独立发布任务执行。

## 契约核对

| 检查项 | 预期 |
| --- | --- |
| 游戏存档键与结构 | 保持不变 |
| 任务键与任务数据 | 保持不变 |
| 背景音乐设置键 | 保持不变 |
| 玩家人数范围 | 保持既有限制 |
| 单个颜色玩家上限 | 保持既有限制 |
| 继续上局入口 | 保持可用 |
| 任务中心入口 | 保持可用 |
| 开始新游戏流程 | 保持可用 |
| 清空全部数据确认 | 保持原有危险操作语义 |
| 恢复默认 | 只重置玩家、声音和动态效果偏好 |

## 主会话验收结果

| 视口或功能 | 结果 |
| --- | --- |
| `1366×768` | 通过；桌面侧边导航和底部操作区完整可见，无横向溢出 |
| `844×390` | 通过；矮横屏紧凑布局完整可用 |
| `390×844` | 通过；DOM 尺寸和横向溢出检查正常 |
| `375×667` | 通过；三类设置均可在小屏操作，无横向溢出 |
| 设置分类切换 | 通过；玩家配置、声音与画面、数据管理可正确切换 |
| 玩家添加、删除、名称和颜色 | 通过；人数、空名称、颜色值和单色上限校验生效 |
| 玩家配置保存与重新加载 | 通过；新增玩家、名称和颜色在重新加载后正确恢复 |
| 背景音乐设置持久化 | 通过；关闭后重新加载仍保持关闭 |
| 动态效果设置持久化 | 通过；关闭后重新加载仍保持关闭，并应用低动态样式 |
| 恢复默认的数据边界 | 通过；只重置玩家、音乐和动态偏好，保留任务与游戏存档 |
| 继续上局和任务中心入口 | 通过 |
| 开始新游戏与覆盖提示 | 通过；开局前先校验并保存玩家配置 |
| 清空全部数据确认 | 保留原有实现；本轮未执行破坏性清空 |
| 页面横向溢出 | 通过；四个目标视口均未发现横向溢出 |
| 浏览器控制台错误 | 通过；最终检查无 `error` 或 `warn` |
| 网页内联脚本语法 | 通过 |
| `git diff --check` | 通过 |

## 助手复核

- 只读助手完成脚本、版本、玩家约束、存储回退、入口绑定和四视口复核，总体结论为 `PASS`。
- 助手未修改正式文件，也未执行 Git 操作。
- 已知低风险项：声音和动态偏好只将字符串 `off` 识别为关闭，其他异常字符串会按开启处理。
- 桌面视口可能出现约 19px 的纵向滚动，但内容没有被裁切，不影响操作。

## 验收门禁

1. 主会话静态检查和四视口运行时检查已完成。
2. 设置持久化与恢复默认数据边界已完成验证。
3. 清空全部数据保留原有确认流程，本轮未执行破坏性测试。
4. 用户已要求进入 Git 发布。
5. 独立发布任务已完成精确白名单审计。

## 当前结论

Web `v2.13.0` C4 设置系统已完成实现和技术验收，并由独立发布任务提交推送。小游戏仍为 `v2.11.1`，尚未同步 C4。
