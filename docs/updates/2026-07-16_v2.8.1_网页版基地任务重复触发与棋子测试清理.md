# v2.8.1 网页版基地任务重复触发与棋子测试清理

日期：2026-07-16
范围：网页版基地任务规则与棋子测试页清理

## 基地任务规则

- 棋子在基地掷出 `2/3/4/5` 时，每次都触发对应任务卡。
- 同一名玩家重复掷到相同点数，也会再次抽取并显示任务。
- 掷出 `1` 仍触发国王卡。
- 掷出 `6` 仍按原规则起飞并获得额外投掷。

## 棋子测试页

- 删除“修改完成”开发说明区块。
- 删除“修改说明”开发说明区块。
- 保留返回游戏入口和红、黄、蓝、绿四种棋子预览。

## 未修改内容

- 不修改任务库内容和任务模式。
- 不修改骰子概率、外圈、直道、棋盘坐标或存档结构。
- 不修改 `minigame/` 小程序文件。

## 版本

- 网页版：`v2.8.1`
- 小程序：保持当前版本，由小程序会话后续按同步包处理。
